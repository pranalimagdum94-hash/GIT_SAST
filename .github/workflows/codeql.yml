name: CI-Coverity-Basic
on:
  push:
    branches: [ main, master, develop, stage, release ]
  pull_request:
    branches: [ main, master, develop, stage, release ]
  workflow_dispatch: 

jobs:
  build:
    runs-on: self-hosted
    steps:
      - name: Checkout Source
        uses: actions/checkout@v4
      - name: Coverity Scan
        uses: blackduck-inc/black-duck-security-scan@v2.3.0
        with:
          ### SCANNING: Required fields
          coverity_url: http://aws-sup-pranalim01.aws.internal:8081/
          coverity_user: admin
          coverity_passphrase: Admin@123
          coverity_project_name: Git_Action 
          coverity_stream_name: Git_Action
          coverity_install_directory: /Users/pranalim/Setup/Coverity/cov-analysis/cov-2025.6.0/                  
          ### Coverity Connect users - Uncomment below
          coverity_local: false
                       
          ### PULL REQUEST COMMENTS:
          coverity_prComment_enabled: true
          github_token: ${{ secrets.GITHUB_TOKEN }} # Required when PR comments is enabled 
                            
